# Python
.venv/
.venv-train/
__pycache__/
*.egg-info/
.pytest_cache/

# Generated / large data (regenerated or downloaded; keep out of git)
data/replays/
data/matrices/
data/selfplay/
*.onnx
*.npz
checkpoints/

.DS_Store
node_modules/
.vgc/
data/snapshots/
data/features/
models/**/*.pkl
models/**/*.pt
models/**/vocab.json

# Kaggle API credentials — never commit
.kaggle-token
kaggle.json
access_token

# Your saved teams: personal state, not part of the tool. Plain JSON — back it up or un-ignore
# this line if you want version history for your teams.
data/library/

# Built frontend (npm --prefix frontend run build). Source lives in frontend/.
src/vgc/web/static/

# TypeScript incremental build state
*.tsbuildinfo
